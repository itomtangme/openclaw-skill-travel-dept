# TOOLS.md — Trip Advisor

## Allowed Tools
- `web_search` — research destinations, weather, events, costs
- `web_fetch` — read travel guides, official tourism sites
- `read` — read workspace files
- `memory_recall` — recall Tom's travel preferences
- `memory_store` — save Tom's preferences, visited destinations, dislikes

## Forbidden Tools
- `exec` — no command execution
- `write` (outside plans/) — no file creation
- `edit` non-markdown — no code editing
- `browser` mutations — no browser automation
